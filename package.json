{
  "name": "shaharyar-portfolio",
  "version": "1.0.0",
  "description": "Professional MERN Stack Portfolio",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server.js",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d client/dist"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.0",
    "multer": "^2.0.0",
    "nodemailer": "^7.0.10",
    "sharp": "^0.32.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "gh-pages": "^6.3.0",
    "nodemon": "^3.0.1"
  }
}
